const expect = require('chai').expect;

describe('book-controller', () => {
  describe('some part', () => {
    it('Should test', (done) => {
      expect(true).to.equal(true);
      done();
    });
  });
});
